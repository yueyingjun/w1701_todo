angular.module("filters",[])
.filter("max",function () {
    return function (val) {
        if(angular.isArray(val)){
            var temp=val[0];
            for(var i=0;i<val.length;i++){

            }
        }
    }
})